# java-calculator-precourse

# 기능 구현 목록
### 기능 1. 문자열을 입력 받은 후 결과값을 출력한다.

### 기능 2. (,) or (:)을 구분자로 가지는 문자열의 경우, 구분자를 기준으로 분리한 각 숫자의 합을 반환한다.

### 기능 3. 커스텀 기본자를 지정할 수 있다.

### 기능 4. 구분자와 양수로 구성된 문자열이 아닌 경우 `IllegalArgumentException` 을 발생시킨 후 종료한다.
